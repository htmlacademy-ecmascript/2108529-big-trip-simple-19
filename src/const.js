const filterTypes = {
  ALL: 'everything',
  FUTURE: 'future'
};

const SortType = {
  DAY: 'sort-day',
  PRICE: 'sort-price'
};

const eventEditDateFormat = 'd/m/y H:i';

export {filterTypes, SortType, eventEditDateFormat};
